[pytest]
# wiser-bqp-cluster/ is a stale duplicate of the project from the first commit. Its
# tests/ share basenames with the real tests/, so collecting both makes pytest abort
# with an "import file mismatch" before running anything.
norecursedirs = wiser-bqp-cluster deck .venv build dist *.egg-info
testpaths = tests
